{
  "name": "nodejs-hmr",
  "version": "1.1.3",
  "main": "./dist/index.js",
  "types": "./src/index",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "start": "ts-node ./test/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^14.6.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  }
}
