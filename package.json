{
  "name":"nodejs-hmr",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./src/index",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^14.6.0"
  }
}
